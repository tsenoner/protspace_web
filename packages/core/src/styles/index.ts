/**
 * ProtSpace Design System
 *
 * Shared styles, design tokens, and reusable mixins for all components.
 * This module provides a consistent design language across the application.
 */

// Export design tokens (CSS custom properties)
export { tokens } from './tokens';

// Export reusable component mixins
export { inputMixin, buttonMixin, dropdownMixin, iconMixin } from './mixins';
